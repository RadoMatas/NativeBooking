import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radoslav.reservationblueprint',
  appName: 'Reservation Blueprint',
  webDir: 'dist'
};

export default config;
